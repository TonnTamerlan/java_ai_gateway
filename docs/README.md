# Goose docs

| Folder | What's in it |
| --- | --- |
| `adr/` | Architectural Decision Records — one per `D-decision`. Append, don't rewrite. |
| `steps/` | Per-step plans + handoff notes. `CHANGELOG.md` is the cross-session log. |
| `diagrams/` | Mermaid sources (`.mmd`). |
| `meetup/` | Talk outline + screenshots (filled in Step 13). |

`CLAUDE.md` at the repo root is the operational guide. Read it first.
